import { handleRemove } from '../src/client/js/app';

describe("Testing the 'remove trip' functionality", () => {
    test('Testing the handleRemove() function', () => {
        expect(handleRemove).toBeDefined();
    });

});